var worker={
    age:23,
    gender:'female'
}

var manager={
    age:43,
    gender:'female'
}

// in this way create two group of data having same variable
// in object variale normaly called property or key 

console.log(manager)
console.log(worker) // calling object

// calling specific property of object

console.log(manager.age)
manager.salar=232;

console.log(manager) // in this way we can add property outside of obhect too