//we say function is a block of code and this code have specific functionality
//example we hava a function that add two number

function addNum(){
    let a=4;
    let b=3;
    let c=a+b
    console.log(c)
}
addNum()

// so we how function syntax is declare 
// first we write function than function name in above example function name is addNum
// inside carly braces is function body
// next we see how we call the function 

// so inside the braces of function we pass the parameter like this

function addNumb(a,b){
    let c=a+b
    console.log(c)
}
addNumb(3,6) // and here we pass argument 

// necessary to learn the terminology what is argument parameter invoke of function function body